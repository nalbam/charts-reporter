version: 2
jobs:
  report:
    docker:
      - image: opspresso/builder
    steps:
      - checkout
      - restore_cache:
          keys:
          - v1-versions-{{ .PreviousBuildNum }}
      - run:
          name: Check Versions
          command: ./report.sh $CIRCLE_PROJECT_USERNAME $CIRCLE_PROJECT_REPONAME $GITHUB_TOKEN
      - store_artifacts:
          path: .versions
      - save_cache:
          paths:
            - .versions
          key: v1-versions-{{ .BuildNum }}

workflows:
  version: 2
  apply:
    jobs:
      - report
  nightly:
    jobs:
      - report
    triggers:
      - schedule:
          cron: "0 0 * * *"
          filters:
            branches:
              only:
                - master
